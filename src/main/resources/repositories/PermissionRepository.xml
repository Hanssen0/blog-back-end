<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="tk.handsome0hell.blog.permission.PermissionRepository">
  <select id="IsUserHasPermission" resultType="Boolean">
    SELECT EXISTS (
      SELECT * FROM
        accounts INNER JOIN permissions
          ON accounts.role_id = permissions.role_id
        WHERE accounts.id = #{user_id} AND
          permissions.permission = #{permission_id}
    )
  </select>
</mapper>
