<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="tk.handsome0hell.blog.users.UsersRepository" >
  <select id="GetUserByUsernameAndPassword"
          resultType="tk.handsome0hell.blog.pojo.User">
    SELECT
      id
    FROM accounts
      WHERE
        username = #{username} and
        password = #{password}
      LIMIT 1
  </select>
  <select id="GetUsers" resultType="tk.handsome0hell.blog.pojo.User">
    SELECT
      id,
      username,
      password,
      role_id
    FROM accounts
  </select>
  <insert id="AddUser">
    INSERT INTO accounts
      (username, password)
    VALUES
      (#{username}, #{password})
  </insert>
  <delete id="DeleteUserById">
    DELETE FROM accounts
    WHERE
      id = #{id}
  </delete>
  <update id="UpdateUser">
    UPDATE accounts
    <set>
      <if test="username != null">username = #{username},</if>
      <if test="password != null">password = #{password},</if>
    </set>
    WHERE id = #{id}
  </update>
</mapper>
